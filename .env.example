# Porta do servidor (Railway define automaticamente)
PORT=3000

# Ambiente
NODE_ENV=production

# Origem permitida para CORS (URL do seu app ou * para permitir todos)
CLIENT_ORIGIN=*

# PostgreSQL - Railway cria essas variáveis automaticamente
# Para desenvolvimento local, configure você mesmo
DB_USER=postgres
DB_PASSWORD=sua_senha
DB_HOST=localhost
DB_PORT=5432
DB_NAME=abyss_connect

# Railway gera automaticamente como DATABASE_URL
# DATABASE_URL=postgresql://user:password@host:port/database
